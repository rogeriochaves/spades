{
  "name": "generate",
  "version": "0.0.1",
  "description": "Generate code for elm projects",
  "main": "index.js",
  "scripts": {
    "watch": "watch 'npm run build' src",
    "build": "elm make src/Main.elm --output out/elm.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "elm": "^0.18.0",
    "elm-format": "^0.6.1-alpha",
    "jsdom": "^11.11.0"
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
